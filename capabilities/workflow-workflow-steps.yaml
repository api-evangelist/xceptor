naftiko: 1.0.0-alpha2
info:
  label: Xceptor Workflow API — Workflow Steps
  description: 'Xceptor Workflow API — Workflow Steps. 2 operations. Lead operation: Xceptor List Workflow Steps. Self-contained
    Naftiko capability covering one Xceptor business surface.'
  tags:
  - Xceptor
  - Workflow Steps
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    XCEPTOR_API_KEY: XCEPTOR_API_KEY
capability:
  consumes:
  - type: http
    namespace: workflow-workflow-steps
    baseUri: https://api.xceptor.com/v1
    description: Xceptor Workflow API — Workflow Steps business capability. Self-contained, no shared references.
    resources:
    - name: workflows-workflowId-steps
      path: /workflows/{workflowId}/steps
      operations:
      - name: listworkflowsteps
        method: GET
        description: Xceptor List Workflow Steps
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: addworkflowstep
        method: POST
        description: Xceptor Add Workflow Step
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.XCEPTOR_API_KEY}}'
  exposes:
  - type: rest
    namespace: workflow-workflow-steps-rest
    port: 8080
    description: REST adapter for Xceptor Workflow API — Workflow Steps. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/workflows/{workflowid}/steps
      name: workflows-workflowid-steps
      description: REST surface for workflows-workflowId-steps.
      operations:
      - method: GET
        name: listworkflowsteps
        description: Xceptor List Workflow Steps
        call: workflow-workflow-steps.listworkflowsteps
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: addworkflowstep
        description: Xceptor Add Workflow Step
        call: workflow-workflow-steps.addworkflowstep
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: workflow-workflow-steps-mcp
    port: 9090
    transport: http
    description: MCP adapter for Xceptor Workflow API — Workflow Steps. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: xceptor-list-workflow-steps
      description: Xceptor List Workflow Steps
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: workflow-workflow-steps.listworkflowsteps
      outputParameters:
      - type: object
        mapping: $.
    - name: xceptor-add-workflow-step
      description: Xceptor Add Workflow Step
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: workflow-workflow-steps.addworkflowstep
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
